import { RestCountryApiModel } from './rest-country-api-model';

describe('RestCountryApiModel', () => {
  it('should create an instance', () => {
    expect(new RestCountryApiModel()).toBeTruthy();
  });
});
